# Optional real backend. When unset, the app runs on the built-in mock database.
VITE_API_URL=

# Cloudinary unsigned upload (Media Library). When unset, uploads are stored as data URLs.
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=
